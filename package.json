{
  "name": "video-to-structured-material-skill",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Turn public video links or transcript JSON into traceable SRT, custom breakdown JSON, Excel, and optional Feishu sheets.",
  "scripts": {
    "setup:video": "node scripts/setup-video-tool.mjs",
    "run": "node scripts/run.mjs",
    "test": "node --test",
    "check": "node --check scripts/run.mjs && node --check scripts/setup-video-tool.mjs && node --test"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
